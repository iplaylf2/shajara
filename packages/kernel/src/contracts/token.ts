/** Type witness used by sigils to hide their echo type. */
export declare const ECHO_TOKEN: unique symbol;

/** Type witness used by opaque keys. */
export declare const KEY_TOKEN: unique symbol;

/** Type witness used by runtime references. */
export declare const REF_TOKEN: unique symbol;
