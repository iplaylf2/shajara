{
  "name": "shajara",
  "workspaces": [
    "internal/*",
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -At --topological run build",
    "format": "yarn run oxfmt --write .",
    "lint": "yarn workspaces foreach -Ap run lint",
    "typecheck": "yarn workspaces foreach -Ap run typecheck"
  },
  "devDependencies": {
    "oxfmt": "^0.39.0",
    "oxlint": "catalog:",
    "typescript": "catalog:"
  },
  "dependenciesMeta": {
    "oxfmt": {
      "unplugged": true
    },
    "oxlint": {
      "unplugged": true
    },
    "tinypool": {
      "unplugged": true
    }
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "yarn@4.13.0"
}
