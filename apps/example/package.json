{
  "name": "@shajara/example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "vite build",
    "dev": "vite dev",
    "lint": "oxlint",
    "typecheck": "yarn run -T tsc --noEmit"
  },
  "dependencies": {
    "@shajara/host": "workspace:^"
  },
  "devDependencies": {
    "@shajara/presets": "workspace:^",
    "oxlint": "catalog:",
    "vite": "catalog:"
  }
}
